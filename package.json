{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "dockerstop": "docker compose stop && cd ./payment-processor && docker compose stop"
  },
  "dependencies": {
    "elysia": "latest",
    "postgres": "^3.4.4",
    "redis": "^4.6.13"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}